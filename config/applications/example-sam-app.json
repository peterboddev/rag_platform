{
  "$schema": "../../lib/constructs/application-pipeline-construct.ts#ApplicationPipelineConfig",
  "_comment": "Example configuration for a SAM-based application",
  
  "applicationName": "example-sam-app",
  "team": "application-team",
  
  "sourceRepo": {
    "owner": "your-github-org",
    "repo": "your-sam-app",
    "branch": "main"
  },
  
  "buildConfig": {
    "runtime": "20",
    "commands": [
      "echo 'Installing dependencies...'",
      "npm ci",
      "echo 'Running tests...'",
      "npm run test --if-present",
      "echo 'Building SAM application...'",
      "sam build",
      "echo 'SAM build completed successfully'"
    ],
    "environment": {
      "NPM_CONFIG_CACHE": "/tmp/.npm"
    }
  },
  
  "_templatePathComment": "For SAM applications, the templatePath field can be omitted. The pipeline will automatically use 'template.yaml' as the default, which is the standard SAM convention.",
  
  "deploymentTargets": ["dev", "staging", "prod"],
  "_deploymentTargetsComment": "List of environment names to deploy to. These must match environment names defined in cdk.json or config/environments/. The pipeline will automatically resolve account, region, and other environment-specific settings from the environment configuration.",
  
  "enabled": true
}
